const create = require('./create');
const update = require('./update');
const remove = require('./remove');
const getAll = require('./getAll');
const filterPoint = require('./filterPoint');

module.exports = {
    create,
    update,
    remove,
    getAll,
    filterPoint
};
